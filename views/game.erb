<h1>Blackjack</h1>

<p><%= session[:user_name]%>, you have chosen to bet $<%=session[:bet] %>.</p>

<div class="well" id="dealer">
  <h4>Dealer cards:</h4>
  <img src="/images/cards/cover.jpg" border="0" />
    <%= display_card(session[:dealer_cards][1]) %>
</div>

<div class="well" id="player">
  <h4>Player cards:</h4>
  <% session[:player_cards].each do |card| %>
    <%= display_card(card) %>
  <% end %>
</div>

<% if @show_buttons == true %>
  
  <% if total(session[:player_cards]) == 21 %>
    <h3><%= session[:user_name] %> hit blackjack!!! Game over.</h3>
    <% break %>
  <% end %>
  
  <h3>Player has <%= total(session[:player_cards]) %>. Hit or stay?</h3>
  <form id="hit" action="/player/hit" method="POST">
    <input type="submit" value="Hit" class="btn btn-success btn-large">
  </form>
  <form id="stay" action="/player/stay" method="POST">
    <input type="submit" value="Stay" class="btn btn-warning btn-large">
  </form>

<% end %>
